# Current Work

> **Generated file.** Holds the one feature, fix, or rollback being built right now. Run
> `feature` to spec a build-plan item, `fix` for an unplanned bug or change,
> or `rollback` to plan a safe reversal. Build one thing at a time;
> `complete` archives it under `religion/history/` and resets this file.

_Nothing in progress. Run `feature`, `fix`, or `rollback` to start._
