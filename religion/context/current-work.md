# Current Work

> **Generated file.** Holds the one feature, fix, rollback, or refactor campaign being
> built right now. Run `feature` to spec a build-plan item, `fix` for an
> unplanned bug or change, `rollback` to plan a safe reversal, or `refactor` to
> simplify existing code. Build one thing at a time; `complete` archives it under
> `religion/history/` and resets this file.

_Nothing in progress. Run `feature`, `fix`, `rollback`, or
`refactor` to start._
