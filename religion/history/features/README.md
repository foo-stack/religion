# Completed features

One file per completed feature, named `NN-slug.md`, where `NN` is its build-plan number.
Written by `complete` when the work lands, and never edited afterwards: this is the
record of what was built and what was known at the time.

Each archive holds the spec as it stood at completion, plus a `## Findings` section listing
the findings resolved with that work.
