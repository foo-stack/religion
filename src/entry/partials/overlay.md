{{product}} is a workflow layer, not an application skeleton. To start a new project,
scaffold the app first in an empty folder, then install {{product}} on top. Never run a
framework scaffolder inside a directory that already holds these files; it fails because
the directory is not empty.
