# Findings

> **Generated file.** The findings ledger: review findings raised by {{cmd:audit}} against
> the work in progress, each with a durable identifier, a severity from P0 to P3, and a
> status. {{cmd:implement}} marks a repair `fixed`, a later {{cmd:audit}} pass moves it to
> `closed`, and {{cmd:complete}} refuses to finish while any P0 or P1 finding is `open` or
> `fixed`, then archives the resolved ones with the work and resets this file.

_No findings recorded. {{cmd:audit}} appends findings here when it finds them._
