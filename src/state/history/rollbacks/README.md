# Rollbacks

One file per reversal, named `YYYY-MM-DD-NN-slug.md` for the item that was rolled back.
Written by {{cmd:complete}}.

The original feature archive stays exactly where it was. A rollback adds a record; it never
rewrites history to make a feature look as though it never existed.
