# Reference material

Screenshots and images that a spec points at, for visual or replication work. Prose
underspecifies a visual target, so {{cmd:feature}} asks for an image and saves it here
rather than building from a description.

Delete anything whose work has landed and whose look is now the code.
